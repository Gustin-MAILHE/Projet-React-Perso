import moon from "../assets/images/moon.svg";
import sun from "../assets/images/sun.svg";


export const BtnTheme = (props) => {
    /*this.addEventListener('click', () => {
        let theme = document.documentElement.dataset.theme;
        if (theme === 'light') {
            theme = 'dark';
        }
        if (theme === 'dark') {
            theme = 'light';
        }
    })*/


}